<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mdanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".RecyclerViewActivity"></activity>
        <activity android:name=".TutorSplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NotesFrontPage"></activity>
        <activity android:name=".SplashActivity" />
        <activity android:name=".CircularRevealAnimation" />
        <activity android:name=".ExplodeTransitionActivity" />
        <activity android:name=".CircularRevealActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".SharedElementActivity" />
        <activity android:name=".RevealAnimation" />
    </application>

</manifest>